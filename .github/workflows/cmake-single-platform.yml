name: HolonNPU CI

on:
  push:
    branches: ["master"]
    tags: ["v*"]
  pull_request:
    branches: ["master"]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: holonnpu-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CMAKE_PRESET: debug
  CC: gcc-15
  CXX: g++-15
  CTEST_OUTPUT_ON_FAILURE: "1"

jobs:
  build-test-lint:
    name: Build, test, lint
    runs-on: ubuntu-26.04
    timeout-minutes: 45

    steps:
      - name: Check out sources
        uses: actions/checkout@v7

      - name: Install toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential \
            gcc-15 \
            g++-15 \
            python3 \
            python3-pip \
            cmake \
            verilator
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - name: Show tool versions
        run: |
          cmake --version
          verilator --version
          "${CC}" --version
          "${CXX}" --version
          python3 --version

      - name: Configure
        run: cmake --preset "${CMAKE_PRESET}"

      - name: Build
        run: cmake --build --preset "${CMAKE_PRESET}" --parallel 2

      - name: Test
        run: ctest --preset "${CMAKE_PRESET}" --output-on-failure

      - name: RTL lint
        run: cmake --build --preset "${CMAKE_PRESET}" --target v1_lint --parallel 2

      - name: Regression build
        run: cmake --build --preset regression --parallel 2

      - name: Regression test
        run: ctest --preset regression --output-on-failure

      - name: Upload CTest logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: ctest-logs
          if-no-files-found: ignore
          path: |
            build/debug/Testing/Temporary/LastTest.log
            build/debug/CMakeFiles/CMakeConfigureLog.yaml
