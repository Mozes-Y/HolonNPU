# include

Shared host and firmware ABI headers live here.

- `my_npu_regs.h`: v1 register, bit, reset, and error-code constants.
- `my_npu_desc.h`: v1 GEMM descriptor layout and descriptor-build constants.
